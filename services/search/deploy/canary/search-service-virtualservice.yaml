apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: search-service
spec:
  hosts:
    - "*"
  gateways:
    - search-service-gateway
  http:
    - route:
        - destination:
            host: search-service
            subset: stable
          weight: 90
        - destination:
            host: search-service
            subset: canary
          weight: 10
