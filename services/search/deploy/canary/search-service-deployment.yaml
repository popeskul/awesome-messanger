apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  labels:
    app: search-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: search-service
      version: canary
  template:
    metadata:
      labels:
        app: search-service
        version: canary
    spec:
      containers:
        - name: search-service
          image: search-service:latest
          ports:
            - containerPort: 8081
          env:
            - name: SERVER_ADDRESS
              valueFrom:
                configMapKeyRef:
                  name: search-service-config
                  key: SERVER_ADDRESS
