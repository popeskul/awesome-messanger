apiVersion: v1
kind: Service
metadata:
  name: search-service
spec:
  selector:
    app: search-service
    version: green # Change to blue to switch
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
