apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service-green
  labels:
    app: profile-service
    version: green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: profile-service
      version: green
  template:
    metadata:
      labels:
        app: profile-service
        version: green
    spec:
      containers:
        - name: profile-service
          image: profile-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          env:
            - name: SERVER_ADDRESS
              value: "0.0.0.0:8080"
